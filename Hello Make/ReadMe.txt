ReadMe:
- This is C compilation example using Makefile.
- MinGW GCC compiler needs to be pre-installed.
- Also Make also needs to be pre-installed.
- Example showing automation of generating main.i, main.s, main.o and a.out files using one command in cmd prompt i.e., make all
- Also make clean cmd built to clean or delete previously generated files.