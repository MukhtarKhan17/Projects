ReadMe:
- This is C compilation example using CMakefile.
- MinGW GCC compiler needs to be pre-installed.
- Also CMake also needs to be pre-installed.
- Example showing automation of generating makefile 